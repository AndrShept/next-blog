/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['assets.contenthub.wolterskluwer.com','images.unsplash.com', 'images.pexels.com', 'ix-www.imgix.net']
    }
}

module.exports = nextConfig
